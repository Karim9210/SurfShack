{{ form_start(form) }}
{{ form_widget(form) }}
<button class="btn btn-primary bg-mainBlue">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
{# <button type="submit" class="btn btn-primary">Delete</button> #}
